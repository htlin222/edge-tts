# MCQ 個人金鑰 —— 這個 repo 是 public，絕對不能進版控
.env

# 合成產物：一集約 10 MB，100 集就 1 GB。只走 release，不進 git。
dist/

# 本機預覽的暫存
build/*-preview.speech.txt
build/preview.speech.txt

.venv/
__pycache__/
*.pyc
uv.lock

# CI 的中間產物，不進版控
plan.json
synth.json
site/

# 你的 ~/.gitignore_global 第 34 行有 build/，會把朗讀稿與 manifest 整個擋掉。
# 本機看不到、CI（runner 沒有全域 ignore）看得到 —— 這種不一致最難查，明確覆蓋掉。
!build/
